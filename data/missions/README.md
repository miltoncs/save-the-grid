# Mission Data

- `campaign-missions.index.json`: canonical ordered list of campaign mission IDs used by terrain-generation tooling.

Runtime mission definitions remain authored in `src/data.content.js`.
