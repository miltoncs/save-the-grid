# Terrain Compatibility Wrappers

This folder contains optional wrapper entry points kept for compatibility with older paths.

Canonical scripts:

- `../generate_terrain_map_png.py`
- `../generate_mission_terrain_maps.py`

Both wrapper scripts here delegate directly to the canonical implementations.
